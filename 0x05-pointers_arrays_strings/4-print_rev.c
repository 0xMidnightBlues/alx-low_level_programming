#include <stdio.h>

/**
 * print_rev - prints a string, in reverse, followed by a new line.
 * @s: string to reverse
 * Return: nothing
 */

void print_rev(char *s)
{
	int i;

	i = 0;
	while (str[i] != '\0')
	{
		i++
	}
	while (s[--i] != '\0')
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
